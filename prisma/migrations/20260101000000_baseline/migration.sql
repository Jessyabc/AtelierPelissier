-- Baseline: production DB existed before Prisma Migrate was used (e.g. prisma db push).
-- Mark as applied with: prisma migrate resolve --applied 20260101000000_baseline
-- (no schema changes — SELECT is a harmless no-op if this file ever runs)
SELECT 1;
